# SpinKorea audited tool/content release

Updated: 2026-09-20 08:55 대한민국 표준시. State: LOCAL VERIFIED; Git push and production reconciliation pending.

## Scope and authority

User requested deployment. Release includes the prior local SPK-001–013 repairs and the now-implemented SPK-014 correction matrix. Normal GitHub main push is authorized; direct Vercel CLI/API/hooks, linking, domain/env/account changes, workflow dispatch, DB writes, index submission, and messages are not part of this execution.

Baseline main: 97b2c0d (origin matched after fetch). Recovery patch and baseline hashes: output/spk014-release/baseline-tracked.patch and baseline-hashes.json. Keep unrelated .omc, .playwright-cli, audit vite-cache, draft prompts, and output evidence untracked.

## Result

- JSON preserves unsafe numeric literals and rejects invalid syntax; percentage invalid inputs clear stale output; four clipboard flows wait for confirmed resolution and report denied/unavailable errors.
- Loan handles zero rate, valid integer periods, numerical limits and both repayment methods. Sleep, password, text-count and Yes/No outputs now describe their actual scope; fictional random-choice example is usable. Prior details: local-repairs-20260920.md.
- exercise-brain, bmi-limitations and ai-era-skills use the audited primary sources and bounded claims. Article dates/slugs/indexing remain unchanged. Six article title/description corrections across the combined release match both metadata caches.
- BMI now distinguishes adult 25–<30, 30–<35 and >=35 classes, aligns six-part gauge/legend/FAQ, rejects nonfinite/nonpositive inputs, and classifies using unrounded values. Display truncation near boundaries is disclosed. Sources and adult-only limits are visible.
- Contact/privacy share accurate static and SPA guidance. Public Issues are for public bug reports; no private rights-request inbox is claimed. Actual private inbox/CMP/AdSense/GSC/account/crawler state remains unverified.
- Fresh build emits 689 article bodies. Public feeds include already-due scheduled content without changing publish timestamps or review/source classifications.

## Fresh verification

- npm run build: PASS, TypeScript + Vite + 689 static article renders. npm run type-check passed during implementation; lint is the same tsc --noEmit command, not an independent lint engine.
- verify-loan, verify-tool-reliability, verify-roulette-storage, verify-spin-consistency: PASS.
- content:validate: PASS (650 generated, 50 approved editorial, minimum score88). verify:growth25, verify-search-scope18 and verify-audit-repairs: PASS.
- verify-tool-reliability-browser: 32/32 PASS, pageerrors0 (isolated development server, simulated clipboard outcomes).
- verify-spk014: 30/30 PASS, pageerrors0 (real built production preview, 390px/1280px). Checks all BMI thresholds/near-boundaries/invalid recovery, three article body/head/canonical/index/sitemap/date parity and horizontal overflow. Initial baseline reproduced missing BMI30/35 labels. Test selector ambiguity between live status and gauge labels and multiline source parsing were corrected before the final pass.
- All727 metadata rows preserve slug/date/source/publishAt/reviewStatus versus baseline HEAD; both caches checked independently. Source post fields and original dates are also asserted for the three SPK-014 articles.
- Mobile BMI screenshot visually inspected; no clipped result/legend. git diff --check PASS.
- Evidence: output/spk014-release/{build.log,browser.json,bmi-390.png}, output/tool-reliability/browser-results.json. Verifiers close their owned browser/server. No advertisements clicked or external APIs exercised by local tests.

## Independent review and limits

Two bounded Luna/max read-only lanes reviewed the prior release diff/deployment path and the SPK-014 source/tool changes. The existing Spark availability failure was preserved; no repeat Spark launch. Privacy contact mismatch was fixed in shared SPA/static copy. Reviewer requests for date/index checks and a narrower BMI FAQ were implemented. No remaining BLOCKER/HIGH was reported for reviewed scope.

Research evidence and population/endpoint caveats remain in spk014-source-review-20260920.md. Static checks do not prove real account settings, private inbox delivery, Google crawler behavior, or real OS clipboard. Historical unrelated security risk records are not re-audited by this release.

## Next action

Stage only reviewed product/generated/test/report files, commit, re-fetch origin, push normally to main, then reconcile GitHub exact SHA, checks/deployment and live public pages. If rollback is needed, use a focused revert commit; do not reset/force-push or restore the whole baseline patch over later work.
